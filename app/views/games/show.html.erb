<h1>Game #<%= @game.id %></h1>

<p>
    Players in game:
    <% @game.players.each do |pl| %>
        <%= pl.name %>
    <% end %>
</p>

<p>
    Dominos available:
    <%= @game.in_game_dominos.map{ |d| d.value }.join(', ') %>
</p>

<p>
    You have <%= @game.current_worm_score %> worms.
</p>

<h2>Turn #<%= @game.current_turn.index %></h2>

<%= show_picked_dice %>
<%= show_roll_outcome %>
<%= show_roll_form %>

<%= link_to 'Quit', root_path %>
